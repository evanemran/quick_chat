class User {
  String userName;
  String lastSeen;
  String bio;
  String mail;
  String mobile;
  String image;

  User(this.userName, this.lastSeen, this.bio, this.mail, this.mobile,
      this.image);
}